# 后端 Dockerfile
FROM python:3.13-slim

WORKDIR /app

# 1. 先只复制依赖文件
COPY backend/requirements.txt .

# 2. 安装依赖
# 这一步之后，正确的依赖就已经安装好了
RUN pip install --no-cache-dir -r requirements.txt

# 3. 最后再复制所有后端代码
# 这样可以确保不会用旧的 requirements.txt 覆盖掉任何东西
COPY backend/ .

# 暴露端口 (虽然Render会自动处理，但这是个好习惯)
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]